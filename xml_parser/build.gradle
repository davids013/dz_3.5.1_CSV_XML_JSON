plugins {
    id 'java'
}

group 'ru.netology'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation project(":core")
}

configurations {
    jar.archiveName = "XML-JSON_parser-" + version + ".jar"
}